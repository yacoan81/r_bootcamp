install.packages("tidyverse")
install.packages("data.table")
install.packages("lubridate")
install.packages("skimr")
install.packages("dplyr")


# data(mtcars)
# head(mtcars)
# tail(mtcars)
# str(mtcars)
# length(mtcars)
# nrow(mtcars)
# dim(mtcars)
# class(mtcars)
# 
# names(mtcars)
# colnames(mtcars)
# summary(mtcars)
# 
# mtcars['mpg'] #data.frame
# mtcars[1] #data.frame
# mtcars$mpg #vector/mumeric
# mtcars[[1]] #vector/mumeric
# 
# max(mtcars[1])
# max(mtcars[['mpg']])
# max(mtcars['mpg'])
# max(mtcars$mpg)
# subset(mtcars, mpg >30)
# 
# library(dplyr)
# mtcars %>% filter(mpg>30)
# 
# mtcars[c("mpg","hp")]
# mean(mtcars$hp)
# mean(mtcars[[1]])
# mean(mtcars[1]) #data.frame does not work

